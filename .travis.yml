language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-mhelvens-library' ]; then cd .. && eval "mv $currentfolder generator-mhelvens-library" && cd generator-mhelvens-library; fi

