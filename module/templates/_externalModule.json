{
  "type": "external-library",
  "name": "<%= name %>",
  "dir": "<%= dir %>",
  "file": "<%= file %>"
  <% if (typeof variableName !== 'undefined') { %>
      , "var": "<%= variableName %>"
  <% } %>
}
